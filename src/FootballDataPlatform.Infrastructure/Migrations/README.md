# Migrations

This branch adds Team metadata fields (`LogoUrl`, `OfficialWebsiteUrl`).

Run the repository's normal EF Core migration command locally to generate the migration and update the database snapshot before merging this branch.
